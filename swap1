#include <stdio.h>
int main()
{
  int x,y;
  printf("enter the numbers to be swapped:");
  scanf("%d",&x);
  scanf("%d",&y);
  x = x + y;
  y = x - y;
  x = x - y;

  printf("After Swapping: x = %d, y = %d", x, y);

  return 0;
}
